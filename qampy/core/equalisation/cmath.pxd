cdef extern from "math.h" nogil:
    double sin(double)

cdef extern from "math.h" nogil:
    double sinf(double)

cdef extern from "math.h" nogil:
    double log(double)

cdef extern from "math.h" nogil:
    double log2(double)

cdef extern from "math.h" nogil:
    double exp(double)

cdef extern from "math.h" nogil:
    double pow(double, double)

cdef extern from "math.h" nogil:
    float powf(float, float)

